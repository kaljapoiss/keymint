# GitLab Pages — static Keymint site (no build step)
image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html logo.svg words.js public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
